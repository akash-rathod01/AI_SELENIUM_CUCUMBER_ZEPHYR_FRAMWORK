# Sample pytest configuration for enterprise test execution
[pytest]
addopts = -v --alluredir=allure-results --maxfail=5
log_cli = true
log_cli_level = INFO

# Enable xdist for parallel execution
# Usage: pytest -n auto
